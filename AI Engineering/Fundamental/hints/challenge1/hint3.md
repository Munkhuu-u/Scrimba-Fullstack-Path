If nothing renders at all, double-check:

- Is the marked library correctly loaded?
- Are you inputting the AI generated string into marked?
- Are you inserting into the output container's HTML?
- These docs: https://scrimba.com/mdn-innerhtml-examples
