The marked library contains a function with:

- input: Markdown string
- output: HTML string

Think of it as a translator step
that must happen _before_ rendering.

Check our the marked docs here:
https://scrimba.com/links/marked-docs
